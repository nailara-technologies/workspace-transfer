#!/bin/bash
# Workspace status check
# Wrapper for status-check.pl

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
WORKSPACE_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$WORKSPACE_ROOT"
exec perl status-check.pl "$@"

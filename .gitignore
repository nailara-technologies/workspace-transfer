# Workspace files
.DS_Store
*.swp
*~

# Credentials (NEVER commit these)
.credentials
.github-token
credentials.conf

# Session markers (local only)
.initialized

# Build artifacts
*.o
*.so
*.dylib

# Temporary files
/work/
*.tmp
